backend:
  app:
    jwtSecret: backendSecretKey
    jwtExpirationMs: 3600000
    
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/katagonia?serverTimezone=UTC
    username: root
    password: password
    initialization-mode: always
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
      
logging:
  level:
    com.mulyadime: DEBUG